apiVersion: rediscloud.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: redis-cloud-creds
      namespace: default
      key: credentials
